import { IndirizzoSedeLegale } from './indirizzo-sede-legale';

describe('IndirizzoSedeLegale', () => {
  it('should create an instance', () => {
    expect(new IndirizzoSedeLegale()).toBeTruthy();
  });
});
